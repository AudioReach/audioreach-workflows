name: Workflows Checks

on:
  pull_request:
    branches: [ pr ]
  push:
    branches: [ pr ]
  workflow_dispatch:

permissions:
  contents: read
  security-events: write

jobs:
  qcom-preflight-checks:
    uses: Audioreach/audioreach-workflows/.github/workflows/qcom-preflight-checks.yml@pr
    secrets: inherit
