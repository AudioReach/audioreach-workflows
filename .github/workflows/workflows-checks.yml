name: Workflows Checks

on:
  pull_request:
    branches: [ sam ]
  push:
    branches: [ sam ]
  workflow_dispatch:

permissions:
  contents: read
  security-events: write

jobs:
  qcom-preflight-checks:
    uses: Audioreach/audioreach-workflows/.github/workflows/qcom-preflight-checks.yml@sam
    secrets: inherit
