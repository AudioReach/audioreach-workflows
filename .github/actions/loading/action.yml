---
name: Load Parameters
description: Load parameters for the workflow
inputs:
  target_image_file:
    description: JSON file to parse as matrix (e.g., Target_image.json)
    required: true
    type: string
    default: 'target_image.json'
outputs:
  build_matrix:
    description: JSON to be used as a matrix via fromJSON()
    value: ${{ steps.parse_matrix.outputs.build_matrix }}
runs:
  using: 'composite'
  steps:
    - name: Parse Target_image.json to matrix
      id: parse_matrix
      uses: actions/github-script@v7
      with:
        script: |
          const fs = require('fs');
          const path = require('path');
          const p = `${{ github.action_path }}/${{ inputs.target_image_file }}`;
          
          if (!fs.existsSync(p)) {
            core.setFailed(`Target image file not found: ${p}`);
          } else {
            let raw = fs.readFileSync(p, 'utf8');
            let data;
            try {
              data = JSON.parse(raw);
            } catch (e) {
              core.setFailed(`Invalid JSON in ${p}: ${e.message}`);
            }
            
            if (data) {
              const include = [];
              const currentRepo = process.env.GITHUB_REPOSITORY.split('/')[1];
              core.info(`Current repository: ${currentRepo}`);
              
              for (const [target, cfg] of Object.entries(data)) {
                // Check build script existence
                const buildScript = cfg.Build_script;
                const buildScriptAbs = path.join(process.env.GITHUB_WORKSPACE, buildScript);
                const scriptExists = fs.existsSync(buildScriptAbs);
                
                // Check if build is disabled for this repository
                const disabledRepos = cfg.Disabled_repos || [];
                const isDisabledForRepo = disabledRepos.includes(currentRepo);
                
                // Read build options from file
                let buildArgs = '';
                try {
                  const buildOptionsPath = path.join(process.env.GITHUB_WORKSPACE, cfg.Build_args);
                  buildArgs = fs.readFileSync(buildOptionsPath, 'utf8')
                    .toString()
                    .trim()
                    .split('\n')
                    .join(' ');
                } catch (err) {
                  console.log(`❌ Error reading build options file for ${target}: ${err}`);
                }
                
                if (isDisabledForRepo) {
                  core.warning(`Skipping target "${target}" because build is disabled for repository: ${currentRepo}`);
                  continue;
                }
                
                if (!scriptExists) {
                  core.warning(`Skipping target "${target}" because build script not found: ${buildScript}`);
                  continue;
                }
                
                if (!buildArgs) {
                  core.warning(`Skipping target "${target}" because build options file not found or empty: ${cfg.Build_args}`);
                  continue;
                }
                
                include.push({
                  target,
                  ...cfg,
                  target_name: target,
                  test_file: cfg.Test_file,
                  sdk_name: cfg.SDK_name,
                  image_name: cfg.Image_name,
                  machine_name: cfg.Machine_name,
                  build_script: cfg.Build_script,
                  build_args: buildArgs, // Include the actual build arguments instead of the file path
                  testing_enabled: cfg.Testing_enabled
                });
              }
              
              const out = JSON.stringify(include);
              
              // Log included and excluded targets with details
              const includedTargets = include.map(i => {
                return `${i.target} (using build args: ${i.build_args.substring(0, 50)}...)`;
              }).join('\n');
              
              const excludedTargets = Object.keys(data)
                .filter(t => !include.find(i => i.target === t))
                .map(t => {
                  const cfg = data[t];
                  const buildOptionsPath = path.join(process.env.GITHUB_WORKSPACE, cfg.Build_args);
                  const reason = [];
                  if (!fs.existsSync(path.join(process.env.GITHUB_WORKSPACE, cfg.Build_script))) {
                    reason.push('missing build script');
                  }
                  if (!fs.existsSync(buildOptionsPath)) {
                    reason.push('missing build options file');
                  }
                  if ((cfg.Disabled_repos || []).includes(currentRepo)) {
                    reason.push('disabled for this repository');
                  }
                  return `${t} (${reason.join(', ')})`;
                })
                .join('\n');
              
              core.info('Targets included:\n' + (includedTargets || 'none'));
              core.info('Targets excluded:\n' + (excludedTargets || 'none'));
              
              if (include.length === 0) {
                core.warning('No valid targets found for building');
              }
              
              core.setOutput('build_matrix', out);
            }
          }